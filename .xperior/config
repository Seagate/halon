[mero]
# Repository URL (e.g. https://github.com/seagate-ssg/halon.git).
#repo = http://gerrit-sage.dco.colo.seagate.com:8080/mero

# Revision parameter as understood by `git rev-parse`
# (e.g. branch name, commit sha1, HEAD~2).
#revision = master

# Gerrit refspec (e.g. refs/changes/60/17060/1).
# NOTE: `refspec` value is ignored if `revision` is provided.
#refspec =
